spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/devhub_db
    username: devuser
    password: devpassword
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true